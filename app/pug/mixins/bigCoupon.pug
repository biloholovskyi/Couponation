mixin coupons-shop(text,text2, name,name2, rate)
  .coupon-shop
    .left-side
      h3= text
    .right-side
      .top
        h2.name= text2
        .button.getCode Get code
      .bottom
        .uses
          .nameUses= name
          .nameUses= name2
        .rate
          .result= rate
          .icon.like 👍
          .icon.dislike 👎